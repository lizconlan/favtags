%p
  %a{:href=>"http://twitter.com/"+@tweets.first.twitterer_name}
    =@tweets.first.twitterer_name
  - if @tweets.first.twitterer_real_name
    ="(" + @tweets.first.twitterer_real_name + ")"

%div.faves
  - form_tag('/favourites/tag') do
    %div.controls
      =hidden_field_tag :page, params[:page]
      =hidden_field_tag :from, "tweep"
      = select_tag :tag_choices, options_for_select(@tag_options), :onchange => "this.form.submit();"
      %noscript
        =submit_tag :submit
        
    %div.page_info
      -if @current_page > 1
        =link_to "&lt;&lt; Prev", :controller => :favourites, :action => :tweep, :page => @current_page - 1
      = "  Page #{@current_page} of #{@max_page}  "
      -if @current_page < @max_page
        =link_to "Next &gt;&gt;", :controller => :favourites, :action => :tweep, :page => @current_page + 1

    %div.tweepline
      = render :partial => "tweet", :collection => @tweets
  
    %div.page_info
      -if @current_page > 1
        =link_to "&lt;&lt; Prev", :controller => :favourites, :action => :tweep, :page => @current_page - 1
      = "  Page #{@current_page} of #{@max_page}  "
      -if @current_page < @max_page
        =link_to "Next &gt;&gt;", :controller => :favourites, :action => :tweep, :page => @current_page + 1